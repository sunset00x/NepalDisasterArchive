<?php
require_once __DIR__.'/_header.php';require_admin();$page_title='Backup';
if($_SERVER['REQUEST_METHOD']==='POST'){verify_csrf();$tables=['admins','categories','stories','story_revisions','story_images','story_sources','correction_reports','analytics_events','admin_activity'];$sql='-- Nepal Disaster Archive backup '.date('c')."\nSET FOREIGN_KEY_CHECKS=0;\n";foreach($tables as $table){$sql.="DELETE FROM `$table`;\n";$rows=db()->query("SELECT * FROM `$table`")->fetchAll();foreach($rows as $row){$columns='`'.implode('`,`',array_keys($row)).'`';$values=implode(',',array_map(fn($value)=>$value===null?'NULL':db()->quote((string)$value),array_values($row)));$sql.="INSERT INTO `$table` ($columns) VALUES ($values);\n";}}$sql.='SET FOREIGN_KEY_CHECKS=1;';log_admin_activity('backup_download');header('Content-Type: application/sql');header('Content-Disposition: attachment; filename=nepal-disaster-archive-'.date('Ymd-His').'.sql');echo $sql;exit;}
?><div class="content"><div class="card"><h2>Database backup</h2><p>Download a SQL backup of archive data, revisions, reports, analytics, and admin activity.</p><form method="post"><input type="hidden" name="csrf" value="<?=e(csrf_token())?>"><button>Download SQL backup</button></form></div></div><?php require_once __DIR__.'/_footer.php';
